const LANGUAGES = ['en', 'ua'];
const LANGUAGES_MAP = { en: 'English', ua: 'Ukrainian' };
const DEFAULT_LANGUAGE = 'en';
const POST_ITEM_TYPES = ['image', 'text'];
const CARD_SIZES = ['1/4', '1/2', '5'];
const ROLES = ['admin', 'user'];

module.exports = {
  LANGUAGES,
  LANGUAGES_MAP,
  DEFAULT_LANGUAGE,
  POST_ITEM_TYPES,
  CARD_SIZES,
  ROLES,
};
